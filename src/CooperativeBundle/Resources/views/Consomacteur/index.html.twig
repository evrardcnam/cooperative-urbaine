{% extends 'CooperativeBundle::baseconsomacteur.html.twig' %}

{% block body -%}
    <h1>Consomacteur list</h1>

    <table class="table table-condensed">
        <thead>
            <tr>
                <th>Id</th>
                <th>Idconsomacteur</th>
                <th>Jourslivraison</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('consomacteur_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.idConsomacteur }}</td>
                <td>{{ entity.joursLivraison }}</td>
                <td>
                <ul>
                    <li>
                        <a class="btn btn-primary" href="{{ path('consomacteur_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a class="btn btn-primary" href="{{ path('consomacteur_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('consomacteur_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}

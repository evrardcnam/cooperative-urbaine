{# empty Twig template #}
{% extends 'CooperativeBundle::baseadmin.html.twig' %}

{% block body %}
    <h1> Pour prouver que c’est bien cette vue qui est affichée </h1>

<form action="{{ path('fos_user_registration_register') }}" {{ form_enctype(form) }} 
method="POST" class="fos_user_registration_register">
          {{ form_widget(form) }}
        <div>
            <input type="submit" value="{{ 'registration.submit'|trans({}, 'FOSUserBundle') }}" />
        </div>
    </form>
{% endblock %}
